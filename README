Demo Spring application to show the usage of the mapped diagnostic context in logback/slf4j. It further showcases a stdout/stderr interceptor for dynamic logging manipulation.

See an example log output in log.out.